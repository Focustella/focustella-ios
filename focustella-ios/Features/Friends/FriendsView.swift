import SwiftUI

struct FriendsView: View {
    var body: some View {
        VStack {
            Text("준비중")
                .font(.title2)
                .foregroundStyle(.secondary)
        }
        .navigationTitle("Friends")
    }
}

#Preview {
    FriendsView()
}

